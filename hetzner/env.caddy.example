# Solo terminación TLS (Caddy). Copiar a .env.caddy — no mezclar con backend/.env ni searxng/.env.searxng
# DNS: A/AAAA de API_DOMAIN y SEARCH_DOMAIN → IP pública Hetzner.

API_DOMAIN=api.tudominio.com
SEARCH_DOMAIN=search.tudominio.com
ACME_EMAIL=tu@email.com
